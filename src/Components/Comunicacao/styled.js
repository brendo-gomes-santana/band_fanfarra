import styled from "styled-components";

export const Main = styled.div`
    position: fixed;
    right: 0;
    bottom: 0;
    
    display: flex;
    flex-direction: column-reverse;

    img{
        width: 70px;
        padding: 0.1em 0.7em;
    }

`;
